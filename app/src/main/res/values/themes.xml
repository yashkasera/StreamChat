<resources>
    <!-- Base application theme. -->
    <style name="Theme.StreamChat" parent="Theme.StreamChat.Base">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange_400</item>
        <item name="colorPrimaryVariant">@color/orange_100</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.StreamChat.Base" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="fontFamily">@font/dosis</item>
        <item name="android:fontFamily">@font/dosis</item>
        <item name="materialButtonStyle">@style/Theme.StreamChat.Button</item>
    </style>

    <style name="Theme.StreamChat.Button" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">4dp</item>
    </style>
</resources>